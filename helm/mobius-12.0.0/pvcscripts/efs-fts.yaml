kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  {{- if .Values.mobius.fts.persistentVolume.enabled }}
  name: {{ .Values.mobius.fts.persistentVolume.claimName }}
  {{- else }}
  name: mobius-pv-fts-claim
  {{- end }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ include "mobius.name" . }}
    helm.sh/chart: {{ include "mobius.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  storageClassName: {{ .Values.storageClassName }}
  accessModes:
    - {{ .Values.mobius.fts.persistentVolume.accessMode }}
  resources:
    requests:
      storage: {{ .Values.mobius.fts.persistentVolume.size }}
  {{- if .Values.mobius.fts.persistentVolume.enabled }}
  volumeName: {{ .Values.mobius.fts.persistentVolume.volumeName }}
  {{- else }}
  volumeName: mobius-pv-fts-storage
  {{- end }}
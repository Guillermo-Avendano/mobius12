kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  {{- if .Values.mobius.mobiusDiagnostics.persistentVolume.enabled }}
  name: {{ .Values.mobius.mobiusDiagnostics.persistentVolume.claimName }}
  {{- else }}
  name: mobius-pv-diagnostics-claim
  {{- end }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ include "mobius.name" . }}
    helm.sh/chart: {{ include "mobius.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  storageClassName: {{ .Values.storageClassName }}
  accessModes:
    - {{ .Values.mobius.mobiusDiagnostics.persistentVolume.accessMode }}
  resources:
    requests:
      storage: {{ .Values.mobius.mobiusDiagnostics.persistentVolume.size }}
  {{- if .Values.mobius.mobiusDiagnostics.persistentVolume.enabled }}
  volumeName: {{ .Values.mobius.mobiusDiagnostics.persistentVolume.volumeName }}
  {{- else }}
  volumeName: mobius-pv-diagnostics-storage
  {{- end }}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  {{- if .Values.master.mobiusViewDiagnostics.persistentVolume.enabled }}
  name: {{ .Values.master.mobiusViewDiagnostics.persistentVolume.claimName }}
  {{- else }}
  name: mobius-pv-diagnostics-claim
  {{- end }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ include "mobius.name" . }}
    helm.sh/chart: {{ include "mobius.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  storageClassName: {{ .Values.storageClassName }}
  accessModes:
    - {{ .Values.master.mobiusViewDiagnostics.persistentVolume.accessMode }}
  resources:
    requests:
      storage: {{ .Values.master.mobiusViewDiagnostics.persistentVolume.size }}
  {{- if .Values.master.mobiusViewDiagnostics.persistentVolume.enabled }}
  volumeName: {{ .Values.master.mobiusViewDiagnostics.persistentVolume.volumeName }}
  {{- else }}
  volumeName: mobiusview-pv-diagnostics-storage
  {{- end }}